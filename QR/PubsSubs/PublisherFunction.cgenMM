void @NAME@_Publish(@ARGS@)
{
		// Create a new message to publish
		auto request_ = @FULLCLASSNAME@();  
		auto request = &request_;
		//fill message
        @ARG_FILL_REQUEST_DATAS@ 

        // Publish the response
        @NAME@->publish(request_);
		
}